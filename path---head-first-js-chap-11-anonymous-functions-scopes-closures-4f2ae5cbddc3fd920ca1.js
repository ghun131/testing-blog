webpackJsonp([0xc58c6de34b96],{356:function(n,e){n.exports={data:{markdownRemark:{html:'<p>1.\nPhần đầu chap này hứa rằng mình sẽ biết một vài \'common coding idioms\' và hai khái niệm <strong>anonymous function</strong> và <strong>closure</strong> được nhấn mạnh.\n<strong>Anoynymous function</strong> là các hàm không tên, cái này không có gì lạ cả. Sách mất một đoạn giới thiệu loại hàm này. Tiếp đến là khái niệm <strong>hoisting</strong> nghĩa là dùng <strong>function declaration</strong> bất kì đầu trong code thay vì viết <strong>function expression</strong> ở trên đầu.</p>\n<p>Mọi thứ có vẻ không có gì thú vị cho đến khi có một câu hỏi. So sánh hai đoạn code sau và giải thích sự khác biệt:</p>\n<h4>Specimen #1</h4>\n<pre><code class="language-javascript">var secret = \'007\';\n\nfunction getSecret() {\n    var secret = \'008\';\n\n    function getValue() {\n        return secret;\n    }\n    return getValue();\n  }\nvar getValueFun = getSecret();\ngetValueFun;\n</code></pre>\n<h4>Specimen #2</h4>\n<pre><code class="language-javascript">var secret = \'007\';\n\nfunction getSecret() {\n    var secret = \'008\';\n\n    function getValue() {\n        return secret;\n    }\n    return getValue;\n  }\nvar getValueFun = getSecret();\ngetValueFun();\n</code></pre>\n<p>Cả hai đoạn code này đều cho cùng một kết quả, khác nhau chỉ la Speciment #1 không return <code>getValue()</code> (chắc tại cái ngoặc tròn làm nó bị vô hiệu hóa) mà chỉ return giá trị của <code>secret</code>. Do vậy lúc đặt <code>var getValueFun = getSecret()</code>, giá trị của <code>getValueFun</code> là giá trị của <code>secret</code> (hay là chuỗi <code>"008"</code>). <code>getValueFun</code> giờ là một string.\nTuy nhiên với Seciment #2 thì <code>getValue</code> được return nên việc đặt <code>var getValueFun = getSecret();</code> giống như đặt hàm <code>getValue()</code> là <code>getValueFun</code>. Lúc này <code>getValueFun</code> là một hàm chứ ko phải một chuỗi như ở Speciment #1, do đó để gọi <code>getValueFun</code> thì phải có ngoặc tròn ở cạnh.</p>\n<p>2.\n<strong>Closure</strong> là một khái niệm được sách nói là phức tạp nhưng quan trọng. Sách có định nghĩa nó là một hàm đồng nhất với môi trường tham chiếu. Khi đó ta nói hàm đó được <em>closed</em> hay đã có một <em>closure</em>.</p>\n<p><em>Dù tự tay viết ra mình vẫn chả hiểu nó là gì, thôi thì chỉ quan tâm xem nó giúp ích được gì và dùng như thế nào</em></p>\n<p><em>Closure</em> có giá trị để hạn chế sử dụng biến <em>global</em>, ví dụ như khi này</p>\n<pre><code class="language-javascript">var count = 0;\n\nfunction counter() {\n    count = count + 1;\n    return count;\n}\n</code></pre>\n<p>Thực ra đoạn code này không có vấn đề gì cả, chỉ là khi làm việc nhóm có thể có nhiều người cùng chọn tên biến là "count". Do vậy nên để biến này là <em>local</em>. Sách có hướng dẫn một cách tiếp cận:</p>\n<pre><code class="language-javascript">function makeCounter() {\n    var count = 0;\n\n    function counter() {\n        count = count + 1;\n        return count;\n      }\n    return counter;\n}\n</code></pre>\n<p>Và thế là chúng ta có <em>closure</em>. Nói ngắn gọn là ta dùng cho tất cả vào hàm <code>makeCounter</code> để rồi return lại hàm <code>counter</code> sau này dùng.\nCách thứ hai để có <em>closure</em> là khi ta dùng một hàm ở trong một hàm khác để xử lý biến tự do (hay biến <em>local</em>). Ví dụ như</p>\n<pre><code class="language-javascript">function makeTimer(doneMessage, n) {\n    setTimeout(function() {\n            alert(doneMessage);\n    }, n);\n}\n</code></pre>\n<p><code>doneMessage</code> là một biến tự do trong khi <code>function() { alert(doneMessage)}</code> là một hàm được truyền vào <code>setTimeout</code>. Vậy là ta đã có <em>closure</em></p>\n<p>Chương này còn nói đến những cái như <em>Lexical scope</em> và <em>free variables</em>. Đoạn đầu có đề cập đến <em>nested function</em>. Chung quy lại với người mới nhập môn như mình những thứ này rất có ích để đọc code đỡ khó chịu và có thể làm quen với syntax của ES6 dễ hơn.</p>',frontmatter:{date:"May 01, 2018",path:"/head-first-js-chap11-anonymous-functions-scopes-closures",title:"Head first JS Chap 11: Anonymous functions, Closures, Scope and Serious Function"}}},pathContext:{}}}});
//# sourceMappingURL=path---head-first-js-chap-11-anonymous-functions-scopes-closures-4f2ae5cbddc3fd920ca1.js.map